version: '3.8'

services:
  telegram-bot:
    build: .
    container_name: telegram-bot-calendar
    restart: unless-stopped
    volumes:
      - ./data:/data
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
    env_file:
      - .env